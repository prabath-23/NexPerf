# NexPerf v0.1

NexPerf v0.1 focuses on a local CLI, a local API, and a dashboard route served by the Go app at `/nexperf`.

The dashboard source lives in `web/` so it can be developed with Vite now and embedded into the Go binary in a later release.
